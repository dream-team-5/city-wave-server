<div id="<%= dom_id tag %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
  <%= tag.name %>

  <span>
    <%= link_to 'View', [:admin, tag], remote: true, class: 'btn btn-outline-info btn-sm' %>

    <%= link_to 'Edit', [:edit, :admin, tag], remote: true, class: 'btn btn-outline-secondary btn-sm' %>

    <%= link_to 'Delete', [:admin, tag], remote: true, method: :delete, class: 'btn btn-outline-danger btn-sm' %>
  </span>
</div>
